**Reproducibility Task**

*Objective:*
Generate code that can be used to reproduce {table} using the methodology in the paper. Use the provided raw data sample to understand the structure of the full raw dataset. *Do not* simply copy paper results.

*Instructions:*
1. Write a Python script for preprocessing, if necessary, that:
    1. Loads any datasets required for reproduction from the provided data files.
    2. Performs all necessary preprocessing steps (cleaning, formatting, handling missing values, type conversions, etc.).
    3. Generates all required intermediate datasets.
    4. Saves each intermediate dataset as a .csv file.
2. Write all necessary Python scripts for reproducing {table} that:
    1. Load the data.
    2. Perform all necessary analyses and calculations.
    3. Generate {table}, formatted exactly as it appears in the paper.
    4. Save {table} in Markdown format as its own individual Markdown file containing *only* the Markdown table and a notes section as a separate Markdown paragraph below the table, if necessary. *Do not* include any extra text. Name the Markdown file '{table}.md'.
    5. Include comments documenting your methodology.
3. Write a bash script that:
    1. Creates a python virtual environment in the current directory.
    2. Creates all necessary directories.
    3. Installs all necessary requirements.
    4. Runs the generated scripts for preprocessing and reproduction.

*Important:*
- Do not copy results from the paper.
- All code must be runnable from start to finish.
- Codes should be well-documented with comments describing your approach.
- Note that you do not have access to the full raw dataset. You only have a sample to understand its structure. This has been provided so that you know how to work with and access features of the full raw dataset. Do not waste time trying to use or understand these values.
- Consider the computational cost of your codes. Do your best to minimize costs and runtimes, but not at the expense of completing your objective.
- Check your scripts for any coding or logic errors.

*Capabilities:*
You are capable of searching the web. Take advantage of this to best complete the objective, when necessary.

*Output Requirements:*
- Each script should be saved in the following format: 
<file="file_path"> 
    file contents 
</file>
